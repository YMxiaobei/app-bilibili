import { OmitStringPipe } from './omit-string.pipe';

describe('OmitStringPipe', () => {
  it('create an instance', () => {
    const pipe = new OmitStringPipe();
    expect(pipe).toBeTruthy();
  });
});
