import { SToTimeStrPipe } from './s-to-time-str.pipe';

describe('SToTimeStrPipe', () => {
  it('create an instance', () => {
    const pipe = new SToTimeStrPipe();
    expect(pipe).toBeTruthy();
  });
});
